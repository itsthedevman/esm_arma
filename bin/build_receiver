#!/usr/bin/env bash

set -euo pipefail

git_root=$(pwd)

cargo build --release --target x86_64-pc-windows-gnu --manifest-path $git_root/src/build/receiver/Cargo.toml

cd $git_root/target/x86_64-pc-windows-gnu/release
mv receiver.exe $HOME/Public/exile_server_manager/esm_build_receiver.exe
