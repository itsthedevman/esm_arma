# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# Ignore the keys
esm.key

# Logs
log/

# 32 bit compiling
SymbolInfo.map

run.env
test_users.yml

node_modules
reset.sql

# Ignore the root config.yml but not the mod's config.yml
config.yml
!src/@esm/config.yml

.env

build_config.yml
*.zip

# Don't ignore - Needed for code to compile without build script
# .build-sha

# Because it's a big mod
server/@exile/addons/*.pbo
server/@exile/addons/*.exile.bisign
server/@exile/addons/*.txt
server/@exile/mod.paa
server/@exileserver/logs
